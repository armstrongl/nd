---
title: "Document undocumented CLI flags and correct stale doc tasks"
id: "oema5w"
status: pending
priority: medium
type: chore
tags: ["docs"]
created_at: "2026-05-17"
dependencies: ["rrts5a", "ahfhih"]
context:
  - "tasks/cli/rrts5a-docs-tier2-content-gaps.md"
  - "tasks/cli/ahfhih-docs-tier3-new-content.md"
  - "cmd/export.go"
  - "cmd/deploy.go"
  - "cmd/profile.go"
  - "cmd/root.go"
  - "cmd/gendocs/main.go"
  - "docs/guide/user-guide.md"
  - "docs/guide/getting-started.md"
  - "docs/guide/creating-sources.md"
  - "docs/guide/asset-types/plugins.md"
  - "docs/guide/glossary.md"
  - ".GitHub/copilot-instructions.md"
  - ".rumdl.toml"
  - "scripts/lint-docs.sh"
verify:
  - type: bash
    run: "go build -o nd ."
  - type: bash
    run: "go run ./cmd/gendocs/ && git diff --exit-code docs/reference/"
  - type: bash
    run: "bash scripts/lint-docs.sh $(git diff --name-only HEAD -- 'docs/guide/*.md' 'docs/guide/**/*.md')"
  - type: assert
    check: "Every nd export and nd export marketplace flag (--name --version --author --email --license --overwrite --owner --plugins --description) has a guide-level explanation with a runnable example in a docs/guide/*.md file"
  - type: assert
    check: "--absolute is documented with a runnable example in at least one docs/guide/*.md file"
  - type: assert
    check: "cmd/gendocs/main.go guideTitles has no unused keys, OR every remaining key is justified in this task's worklog"
---

## Document undocumented CLI flags and correct stale doc tasks

### Objective

Pattern expansion / correction of seed tasks `rrts5a`
(`tasks/cli/rrts5a-docs-tier2-content-gaps.md`) and `ahfhih`
(`tasks/cli/ahfhih-docs-tier3-new-content.md`). Several seed-task claims are
factually wrong or already satisfied (rrts5a T2-4, rrts5a T2-5, ahfhih T3-18,
parts of rrts5a T2-7 / ahfhih T3-19), while the genuinely undocumented surface
(the full `nd export` / `export marketplace` flag set) is enumerated by
neither seed at guide level. This task corrects the stale premises and
documents the real gaps.

Why: a user reading `docs/guide/` today cannot discover the export plugin/
marketplace flags or the `--absolute` symlink override, and the dead
`gendocs` `guideTitles` entries silently mislead future doc work.

Scope notes (verified 2026-05-17 against the repo — do not re-investigate):

- The docs directory is `docs/guide/` (singular). It contains exactly:
  `configuration.md`, `creating-sources.md`, `getting-started.md`,
  `glossary.md`, `how-nd-works.md`, `profiles-and-snapshots.md`,
  `troubleshooting.md`, `user-guide.md`, and `asset-types/` (`_index.md`,
  `agents.md`, `commands.md`, `context.md`, `hooks.md`, `output-styles.md`,
  `plugins.md`, `rules.md`, `skills.md`). There is **no** `docs/guides/`.
- Style rules are authoritative in `.github/copilot-instructions.md:148-157`
  (sentence-case headings; base-verb forms; ` ```shell ` fences, never
  ` ```bash `; `:` not `--` between term and description; tree notation
  `├── └── │`; forbidden words `simple/simply/easy/easily/just/obviously/
  straightforward`; "source"/"asset" not "file"; hedge agent pickup with
  "typically"). Mechanically enforced by `scripts/lint-docs.sh` and
  `.rumdl.toml` (`[MD063] style = "sentence-case"`, `ignore-words` at
  `.rumdl.toml:51`).
- `docs/reference/*.md` is auto-generated by `go run ./cmd/gendocs/` from
  Cobra definitions (`CONTRIBUTING.md:71`; CI:
  `.github/workflows/deploy-docs.yml:50-51`). Do NOT hand-edit reference
  pages — change the Cobra command in `cmd/` and regenerate.
- Sibling task `ahfhih` (this task depends on it) covers the same
  reconciliation in its T3-9/T3-18/T3-19 items and states "coordinate, do not
  duplicate". Do this task's items only if `ahfhih` has not already landed
  them; otherwise verify and mark satisfied. The export/plugin guide work
  (item 1 below) overlaps `ahfhih` T3-9 — if `ahfhih` created
  `docs/guide/export-plugin-workflow.md`, extend it rather than duplicate.

### Tasks

- [ ] **Document the `nd export` flag set in a guide.** None of these appear
  with a guide-level explanation today: `--name --version --author --email
  --license --overwrite` (defined `cmd/export.go:159-168`) plus marketplace
  `--owner --plugins --description` (defined `cmd/export.go:299-305`;
  `--owner` is line 301, `--plugins` line 303, `--description` line 300).
  Current guide coverage is only: `docs/guide/asset-types/plugins.md:54`
  (`nd export --assets ... --output ...`, no other flags) and
  `docs/guide/glossary.md:108-109` (bare `nd export` / `nd export
  marketplace`). Add prose + runnable examples for every flag above. Target:
  `docs/guide/asset-types/plugins.md` "Package assets for distribution"
  section (heading at `docs/guide/asset-types/plugins.md:97`), and/or
  `docs/guide/export-plugin-workflow.md` if `ahfhih` T3-9 created it. Reuse
  the worked examples already in the Cobra `Example` fields
  (`cmd/export.go:41-45` for `nd export`, `cmd/export.go:220-221` for
  `nd export marketplace`). Note the required flags enforced at runtime:
  marketplace requires `--name`, `--owner`, `--plugins`
  (`cmd/export.go:228-236`).
- [ ] **Document `--absolute` in a guide.** Flag defined at
  `cmd/deploy.go:292` (`BoolVar(&absolute, "absolute", ...)`); mutually
  exclusive with `--relative` (`cmd/deploy.go:293`). It appears in **zero**
  `docs/guide/*.md` files today (`--relative`/symlink strategy is covered at
  the "Symlink strategy" heading `docs/guide/user-guide.md:157`). Add an
  `--absolute` example and prose to that "Symlink strategy" section so both
  overrides are documented side by side. This confirms `ahfhih` T3-11's
  `--absolute` sub-item — coordinate, do not duplicate if already landed.
- [ ] **Fix the dead `gendocs` `guideTitles` entries.** The map at
  `cmd/gendocs/main.go:177-193` has **7 unused keys** (NOT 1, and NOT 8 as
  rrts5a T2-7 claims). `guideTitles` is read only at
  `cmd/gendocs/main.go:154` to title cross-links from each command's
  `Annotations["docs.guides"]`. The only slugs referenced by any
  `docs.guides` annotation across `cmd/` are: `getting-started`,
  `configuration`, `creating-sources`, `profiles-and-snapshots`,
  `troubleshooting`, `asset-types/hooks`, `asset-types/plugins`,
  `asset-types/output-styles`. Therefore the unused keys are: `how-nd-works`,
  `glossary`, `asset-types/skills`, `asset-types/agents`,
  `asset-types/commands`, `asset-types/rules`, `asset-types/context`. Either
  delete these 7 dead keys from the map, OR wire them up by adding the
  corresponding slug to a relevant command's `docs.guides` annotation (e.g.
  add `asset-types/skills` to a skills-related command). Whichever path:
  after editing `cmd/gendocs/main.go`, run `go run ./cmd/gendocs/` and commit
  the regenerated `docs/reference/` (must be clean under
  `git diff --exit-code docs/reference/` once committed).
- [ ] **Reconcile rrts5a T2-4 (duplicate "Filter by type" headings).** FALSE
  for the current file: `docs/guide/getting-started.md` has exactly one
  `Filter by type:` — non-heading prose at
  `docs/guide/getting-started.md:101`, followed by `nd list --type skills`.
  There are no duplicate headings. Close rrts5a T2-4 as a non-issue; record
  the verification in this task's worklog
  (`taskmd worklog oema5w --add "..."`). No edit to getting-started.md.
- [ ] **Reconcile rrts5a T2-5 (`source remove --yes` warning).** Already
  satisfied: the destructive-behavior admonition exists verbatim at
  `docs/guide/creating-sources.md:141` ("> **Warning:** `nd source remove
  <id> --yes` skips the interactive prompt and **removes all deployed
  assets**..."). Close rrts5a T2-5 as satisfied; record in worklog. No edit.
- [ ] **Reconcile ahfhih T3-18 (`nd profile create` Example).** Already
  satisfied: `cmd/profile.go:49-53` has a populated Cobra `Example` including
  a `--scope project` example. The `--assets` flag is parsed at
  `cmd/profile.go:91` (`else if assets != ""`), format
  `type/name,type/name,...`. Optionally add one `--assets` line to the
  `Example` at `cmd/profile.go:49-53`; if you do, run `go run ./cmd/gendocs/`
  and commit the regenerated `docs/reference/nd_profile_create.md`. If no
  change, mark satisfied and note in worklog.
- [ ] **Reconcile ahfhih T3-19 / branding source-of-truth.** The "Napoleon
  Dynamite" string lives at exactly one source: `cmd/root.go:23`
  (`Short: "Napoleon Dynamite - coding agent asset manager"`). It propagates
  into **18** of 36 `docs/reference/*.md` pages NOT via the `docs.related`
  annotation (the original task's "via `docs.related`" is imprecise) but via
  the Cobra-generated parent-command backlink under each subcommand page's
  `## Related` section — rendered as
  `- [nd](nd.md) - Napoleon Dynamite - coding agent asset manager`
  (`gendocs` logic around `cmd/gendocs/main.go:101,118,124-137`; example
  output `docs/reference/nd_deploy.md:71`). This task does NOT decide the
  keep/replace/remove question — `ahfhih` T3-19 owns that decision and its
  worklog. Here, only confirm the single-source location and the 18-page
  propagation are accurate, and record the verification in this task's
  worklog. If `ahfhih` already changed `cmd/root.go:23`, do nothing. Note:
  `.rumdl.toml:51` whitelists `"Napoleon"`, `"Dynamite"` in `[MD063]
  ignore-words` — if the words are ever removed, that list must be updated
  (tracked under `ahfhih`).
- [ ] **TUI guide coverage.** `ahfhih` T3-8 (valid) owns creating
  `docs/guide/tui.md`. The bare `nd` command launches the TUI when stdin is
  a terminal and no `--verbose/--quiet/--json` conflicts:
  `cmd/root.go:44-50` (`RunE` → `tui.Run(app)` at `cmd/root.go:47`).
  Implementation lives in `internal/tui/` (entry `internal/tui/tui.go`).
  Today there is **zero** TUI mention in any `docs/guide/*.md` and no
  `docs/guide/tui.md`. This task: verify `ahfhih` T3-8 produced
  `docs/guide/tui.md` (or that it is still outstanding) and record status in
  the worklog. Do NOT author the TUI guide here — defer to `ahfhih` T3-8 to
  avoid duplication.
- [ ] After all doc edits, run `bash scripts/lint-docs.sh <changed guide
  files>` (usage: `scripts/lint-docs.sh [file ...]`,
  `scripts/lint-docs.sh:4`) and fix every `error`-level finding (warnings:
  review, do not necessarily change).

### Acceptance criteria

- Every `nd export` flag (`--name --version --author --email --license
  --overwrite`) and every `nd export marketplace` flag (`--name --description
  --owner --plugins --output --overwrite`) has a guide-level prose
  explanation plus a runnable ` ```shell ` example in at least one
  `docs/guide/*.md` file.
- `--absolute` is documented with a runnable example alongside `--relative`
  in the "Symlink strategy" section of `docs/guide/user-guide.md`.
- `cmd/gendocs/main.go` `guideTitles` (`:177-193`) has no unused keys, OR
  every retained key has a matching `docs.guides` annotation in `cmd/`; the
  decision and any wired slugs are recorded in this task's worklog.
- `go run ./cmd/gendocs/ && git diff --exit-code docs/reference/` is clean
  after any `cmd/` change (no stale generated reference pages).
- Stale seed items are reconciled in the worklog: rrts5a T2-4 closed as
  non-issue (one prose occurrence at getting-started.md:101), rrts5a T2-5
  closed as satisfied (creating-sources.md:141 warning present), ahfhih T3-18
  closed as satisfied (cmd/profile.go:49-53 Example populated). ahfhih T3-19
  and T3-8 status noted with no duplicated work.
- `go build -o nd .` succeeds.
- `bash scripts/lint-docs.sh <changed guide files>` reports zero
  `error`-level findings.

### References

- Seed/coordination tasks: rrts5a —
  `tasks/cli/rrts5a-docs-tier2-content-gaps.md` (Tier 2, T2-4/T2-5/T2-7);
  ahfhih — `tasks/cli/ahfhih-docs-tier3-new-content.md` (Tier 3,
  T3-8/T3-9/T3-11/T3-18/T3-19). This task depends on both; ahfhih's scope
  note explicitly directs coordination with `oema5w`.
- Key code (verified 2026-05-17): `cmd/export.go:41-45,159-168,220-221,228-236,299-305`;
  `cmd/deploy.go:292-293`; `cmd/profile.go:49-53,91`; `cmd/root.go:23,44-50`;
  `cmd/gendocs/main.go:101,118,124-137,154,177-193`.
- Existing guide coverage to extend, not duplicate:
  `docs/guide/asset-types/plugins.md:54,97` (export);
  `docs/guide/glossary.md:103-109` (export glossary entry);
  `docs/guide/user-guide.md:157` ("Symlink strategy");
  `docs/guide/creating-sources.md:141` (`--yes` warning, already present);
  `docs/guide/getting-started.md:101` ("Filter by type:" prose, single).
- Style rules (authoritative): `.github/copilot-instructions.md:148-157`;
  enforced by `scripts/lint-docs.sh` and `.rumdl.toml` (`ignore-words`
  `.rumdl.toml:51`).
- Reference-doc regeneration: `go run ./cmd/gendocs/` (`CONTRIBUTING.md:71`;
  CI `.github/workflows/deploy-docs.yml:50-51`).
